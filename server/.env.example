# Spotify API Configuration
#
# Copy this file to `.env` and fill in your own values.  All URLs must
# start with https:// to satisfy Spotify’s redirect requirements.

CLIENT_ID=your_spotify_client_id_here
CLIENT_SECRET=your_spotify_client_secret_here
# For local development, run your backend on 8888 via HTTPS (e.g. ngrok) and
# point the redirect URI back to your secure endpoint.
REDIRECT_URI=https://localhost:8888/callback
# URL of your React frontend.  Use https://localhost:3000 for development.
CLIENT_URL=https://localhost:3000

# Production URLs (uncomment and edit when deploying)
# REDIRECT_URI=https://your-backend-domain.com/callback
# CLIENT_URL=https://your-frontend-domain.com